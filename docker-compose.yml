services:
  fnnet-monitor:
    build: .
    container_name: fnnet-monitor
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    privileged: true
    pid: host
    volumes:
      - /sys:/sys:ro
    environment:
      - TZ=Asia/Shanghai
      - PORT=5000
